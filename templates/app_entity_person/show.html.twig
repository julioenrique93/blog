{% extends 'base.html.twig' %}

{% block title %}AppEntityPerson{% endblock %}

{% block body %}
    <h1>AppEntityPerson</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ app_entity_person.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ app_entity_person.name }}</td>
            </tr>
            <tr>
                <th>Lastname</th>
                <td>{{ app_entity_person.lastname }}</td>
            </tr>
            <tr>
                <th>Ci</th>
                <td>{{ app_entity_person.ci }}</td>
            </tr>
            <tr>
                <th>Address</th>
                <td>{{ app_entity_person.address }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('person_index') }}">back to list</a>

    <a href="{{ path('person_edit', {'id': app_entity_person.id}) }}">edit</a>

    {{ include('app_entity_person/_delete_form.html.twig') }}
{% endblock %}
